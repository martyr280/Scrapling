[project]
name = "backend"
version = "0.1.0"
description = "Scrapling web interface backend"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "scrapling[fetchers]>=0.4.0",
    "markdownify>=0.14.1",
]
